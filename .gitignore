node_modules/
dist/
*.tmp
*.log
.DS_Store

# The working diagram and its history — live documents, not repo content.
graph.json
*.ops.jsonl
*.state.json

# A directory of diagrams, when the server is pointed at one.
diagrams/
